# Production Environment Variables for Bitbucket Pipeline
# This file is copied to .env before building on production server

# API Base URL (backend is on same domain)
VITE_API_BASE_URL=https://helpdesk.hubblehox.ai/api

# WebSocket URL (secure WebSocket)
VITE_WS_URL=wss://helpdesk.hubblehox.ai
