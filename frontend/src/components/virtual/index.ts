/**
 * Virtual Scrolling Components Index
 * ===================================
 * Re-exports all virtual scrolling components for easy imports.
 *
 * Usage:
 *   import { VirtualTable, VirtualList } from '@/components/virtual';
 */

export {
  VirtualTable,
  VirtualList,
  type VirtualTableColumn,
  type VirtualTableProps,
  type VirtualListProps,
} from './VirtualScrolling';
